{
  "name": "rollup-demo",
  "version": "1.0.0",
  "description": "",
  "main": "bundle.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:webpack": "npx webpack",
    "build:rollup": "rollup --config -w",
    "build": "npm run build:webpack && npm run build:rollup",
    "rollup": "rollup --config ./config/input.config.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.12"
  },
  "dependencies": {
    "rollup": "^2.19.0"
  }
}
